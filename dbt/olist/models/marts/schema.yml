version: 2

models:
  - name: fct_orders
    columns:
      - name: order_id
        tests:
          - unique
          - not_null

  - name: fct_order_items
    columns:
      - name: order_id
        tests:
          - not_null
      - name: product_category_name_english
        tests:
          - not_null
